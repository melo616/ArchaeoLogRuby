<div class="container">
  <h1>Editing announcement</h1>

    <div>
      <%= link_to "Back to announcement", dig_announcement_path(@dig, @announcement) %> |
      <%= link_to 'Back to dig', dig_path(@dig) %> |
      <%= link_to "Announcement list", dig_announcements_path(@dig, @announcement) %>
    </div>

    <hr>

  <%= render "form", announcement: @announcement %>
</div>
